# rebuild-bust: 2026-05-20-phase1-helloworld
# PHASE 1 of a radical bisect — everything stripped to the absolute minimum.
# If THIS doesn't deploy, the failure is at the Streamlit Cloud account/build
# level and only a UI-side reboot + clear-cache can fix it.
# When Phase 1 renders, restore the full requirements list incrementally
# (pandas, plotly, joblib, sklearn, lightgbm, category_encoders, imbalanced-learn, dill).
streamlit
