# Root-level requirements.txt — picked up by Streamlit Cloud at deploy time.
# Keep this file minimal: ONLY what dashboards/churn_dashboard_app.py needs at
# runtime + the deps needed to unpickle the LightGBM joblib payload.
#
# If you change a version here, mirror it in dashboards/requirements.txt and
# serving/requirements-flask.txt to keep all surfaces in sync.

streamlit==1.53.0
pandas==2.2.3
numpy==2.1.3
joblib==1.5.3
plotly==6.7.0

# Required so joblib.load() can rehydrate the trained model:
scikit-learn==1.6.1
lightgbm==4.6.0
category_encoders==2.9.0
imbalanced-learn==0.14.1
dill==0.3.9

# joblib transitives — pin them explicitly so Streamlit Cloud's pip resolver
# does NOT skip them on a fresh Python 3.13 builder that lacks pre-built wheels.
loky>=3.4.0
threadpoolctl>=3.1.0
cloudpickle>=3.0.0
