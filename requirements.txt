# rebuild-bust: 2026-05-20-phase2.1-data-and-viz
# PHASE 2.1 of the radical bisect — restore data + viz stack only.
# Skips the heavy ML stack (lightgbm / category_encoders / imbalanced-learn /
# dill) until Phase 2.2 so a possible install failure of those doesn't mask
# whether pandas + plotly + sklearn are healthy on Cloud's builder.
streamlit==1.53.0
pandas==2.2.3
numpy==2.1.3
joblib==1.5.3
plotly==6.7.0
scikit-learn==1.6.1
